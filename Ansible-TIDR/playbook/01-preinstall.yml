---
- name: Ensure a list of packages installed
  yum:
    name: "{{ packages }}"
  vars:
    packages:
    - gcc
    - openssl-devel
    - bzip2-devel
    - libffi-devel
  become: yes 

# - name: Upgrade all packages on servers
#   yum:
#     name: '*'
#     state: latest