# Install basic packages
#
#

   - name: installng initial packages...
     yum:
        name: "{{ item }}"
        state: latest
     loop:
        - net-tools
        - vim
        - nano
        - wget
        - epel-release
        - mlocate
        - ntpdate
        - zabbix-agent
        - sysstat

